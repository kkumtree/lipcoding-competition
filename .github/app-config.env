# GitHub Actions에서 사용할 설정들

# 백엔드 실행 명령어 (백그라운드)
backend_command="./start_backend.sh"

# 프론트엔드 실행 명령어 (백그라운드) 
frontend_command="./start_frontend.sh"

# API 문서 경로
swagger_ui_url="docs"
openapi_url="openapi.json"

# 포트 설정
backend_port=8080
frontend_port=3000
